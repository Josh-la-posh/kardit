import { describe, it } from 'vitest';

describe.skip('onboarding decision audit logging', () => {
  it('needs to be rewritten for the real onboarding API flow', () => {
    // The previous test covered a removed local mock onboarding implementation.
  });
});
